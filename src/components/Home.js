import React from "react";

const Home = () => {
  const homesDataPromise = Promise.resolve([]);
  return (
    <div>
      <div data-testid="home">sdfl</div>
    </div>
  );
};

export default Home;
